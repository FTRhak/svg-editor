import { TreeContextmenuNoselectionDirective } from './tree-contextmenu-noselection.directive';

describe('TreeContextmenuNoselectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeContextmenuNoselectionDirective();
    expect(directive).toBeTruthy();
  });
});
