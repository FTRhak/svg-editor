import { FieldLockDirective } from './field-lock.directive';

describe('FieldLockDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldLockDirective();
    expect(directive).toBeTruthy();
  });
});
