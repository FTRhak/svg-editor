import { CanvasManualEditDirective } from './canvas-manual-edit.directive';

describe('CanvasManualEditDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasManualEditDirective();
    expect(directive).toBeTruthy();
  });
});
