import { CanvasGridRenderDirective } from './canvas-grid-render.directive';

describe('CanvasGridRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasGridRenderDirective();
    expect(directive).toBeTruthy();
  });
});
